import { View, Text } from 'react-native';
import { EmptyScreen } from '../components/EmptyScreen';

export default function FamilyScreen() {
  return (
    <EmptyScreen
      emoji="👨‍👩"
      title="Aile"
      subtitle="Ece'nin ailesi burada olacak."
    />
  );
}