import { View, Text } from 'react-native';
import { EmptyScreen } from '../components/EmptyScreen';

export default function ComicsScreen() {
  return (
    <EmptyScreen
      emoji="📚"
      title="Çizgi Romanlar"
      subtitle="Ece'nin özel çizgi romanları burada olacak."
    />
  );
}